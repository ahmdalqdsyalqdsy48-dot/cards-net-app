name: cards_net_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.1.2 
  local_auth: ^2.1.6 
  flutter_colorpicker: ^1.1.0 
  firebase_core: ^2.32.0 
  cloud_firestore: ^4.15.8 
  pdf: ^3.11.0          # 👈 مكتبة رسم الفواتير وملفات الـ PDF
  printing: ^5.12.0     # 👈 مكتبة الطباعة والتحميل الفعلي في المتصفح
  intl: ^0.19.0         # 👈 مكتبة ضبط الأوقات والتواريخ المحاسبية

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
