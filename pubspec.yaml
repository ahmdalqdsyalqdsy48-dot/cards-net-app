name: cards_net_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  # قمنا بتحديد النسخة بدقة لتفعيل المميزات الحديثة
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.1.2 # 👈 تم إضافة مكتبة العقل (Provider) هنا بنجاح
  local_auth: ^2.1.6 # 👈 إضافة مكتبة البصمة الحقيقية هنا

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
